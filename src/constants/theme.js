import { colors } from '../style/styleSettings';

const theme = {
  primaryColor: colors.WALLMART_BLUE,
  iconColor: colors.WALLMART_BLUE,
  secondaryTextColor: colors.PRIMARY_DARK_TEXT,
  fontColor: colors.PRIMARY_DARK_TEXT
};

export default theme;
